# Copy to .env and fill in real values (.env is gitignored).
# Generate a fresh secret:  openssl rand -hex 32
SEARXNG_SECRET=replace_with_openssl_rand_hex_32
# Public base URL of the DIRECT instance (used in absolute links). Optional.
SEARXNG_BASE_URL=http://localhost:8890/
