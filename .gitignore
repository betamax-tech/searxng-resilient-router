# Secrets — never commit
.env
searxng/.env

# Generated proxy/instance caches (churny, regenerated by proxypool)
cache/candidates.json
cache/active_pool.json
cache/healthy_instances.json
cache/deadletter/

# Runtime logs
logs/*.log

# Compiled Go binaries (built from source via build.sh)
go/searxng-router
go/proxypool
go/deadletter-retry

# Retired reference scripts
*.disabled

# OS / editor
.DS_Store
*.swp
